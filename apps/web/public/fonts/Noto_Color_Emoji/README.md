/**
 * Noto Color Emoji — flag subset only (regional indicators U+1F1E6–U+1F1FF).
 *
 * Source: Google Fonts “Noto Color Emoji” (COLRv1, Emoji 17 / Noto v39 family),
 * subset via `text=` for the 26 regional-indicator letters so country flags
 * render as color glyphs on Windows (where Segoe UI Emoji shows “VN” letters).
 *
 * Full font is ~25MB TTF / ~5.7MB woff2; this flags-only file is ~775KB.
 * Regenerate: request Google Fonts CSS with Chrome UA + text=🇦…🇿, save woff2.
 */
