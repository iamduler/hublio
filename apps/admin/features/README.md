# Admin features

Mirror `apps/web/features`: pages live under `app/`; feature modules hold
`api.ts`, `components/`, `hooks`, `types.ts` for each admin domain.

| Feature | Status |
| --- | --- |
| `auth` | Login form |
| `organizations` | Stub (next: list / suspend / activate) |
| `connectors` | Stub (next: registry UI) |
